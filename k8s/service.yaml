apiVersion: v1
kind: Service
metadata:
  name: sms-api-service
spec:
  type: LoadBalancer
  selector:
    app: sms-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30005
